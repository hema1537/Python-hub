#include <stdio.h>

int main() {
  int a, b, c;

  printf("\nEnter 2 Nos: ");
  scanf("%d%d", &a, &b);

  c = a + b;
  printf("\nTotal: %d", c);

  c = a - b;
  printf("\nDifference: %d", c);

  c = a * b;
  printf("\nMul: %d", c);

  c = a / b;
  printf("\nDiv: %d", c);

  return 0;
}
